// apps/web/app/(developer)/layout.tsx
import { AppShell } from '@/components/layout/AppShell';

const DeveloperLayout = ({ children }: { children: React.ReactNode }) => <AppShell>{children}</AppShell>;

export default DeveloperLayout;
