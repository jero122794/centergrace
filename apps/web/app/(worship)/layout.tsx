// apps/web/app/(worship)/layout.tsx
import { AppShell } from '@/components/layout/AppShell';

const WorshipLayout = ({ children }: { children: React.ReactNode }) => <AppShell>{children}</AppShell>;

export default WorshipLayout;
