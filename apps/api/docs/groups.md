# Groups module

## Purpose

Leaders create groups freely. A leader may attach a group to a ministry only if they lead that ministry. Adding a member triggers a push notification.

## Endpoints

- `GET/POST /api/groups`
- `GET /api/groups/:id`
- `GET/POST /api/groups/:id/members`
- `DELETE /api/groups/:id/members/:userId`
