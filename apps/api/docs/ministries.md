# Ministries module

## Purpose

Church-wide ministries. Only Admin and Developer can create or edit. Leaders can list ministries they belong to through the same read endpoints.

## Endpoints

- `GET/POST /api/ministries`
- `GET/PATCH /api/ministries/:id`
- `GET /api/ministries/:id/members`
- `GET /api/ministries/:id/stats`
